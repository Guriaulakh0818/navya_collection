'use server';

/**
 * Server action to retrieve current authenticated user.
 * Prepared for custom MSG91 OTP authentication session handling.
 */
export async function getCurrentUser() {
  try {
    return null;
  } catch {
    return null;
  }
}
